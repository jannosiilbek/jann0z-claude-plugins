# Personas (wrong heading level — no `## Personas` with `### <PersonaName>` jobs tables)

This 07 drops the `**Jobs-to-be-done:**` tables entirely, so the (persona, job)
bijection set cannot be built — the harness cannot anchor its resolution target and
must report broken-test (simulation.md §9 case 3).

Some prose about account holders and tellers, but no pinned structure.
