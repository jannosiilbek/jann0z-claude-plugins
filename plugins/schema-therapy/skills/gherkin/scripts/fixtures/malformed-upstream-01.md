# Event Storming

This file drops the `## Domain Events` and `## Actors` tables entirely, so the B7
authorization-obligation set cannot be built — the harness must refuse with broken-test.

## Notes

Nothing to see here.
